<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ActionBarMenuIcon" parent="Widget.AppCompat.ImageButton">
        <item name="android:layout_width">20dp</item>
        <item name="android:layout_height">20dp</item>
        <item name="android:contentDescription">@string/desc</item>
        <item name="android:background">@color/transparent</item>
        <item name="android:scaleType">fitXY</item>
    </style>

    <style name="BottomBarMenuIcon" parent="Widget.AppCompat.ImageButton">
        <item name="android:layout_width">15dp</item>
        <item name="android:layout_height">15dp</item>
        <item name="android:padding">5dp</item>
        <item name="android:contentDescription">@string/desc</item>
        <item name="android:background">@color/transparent</item>
    </style>

    <style name="NoteTimeDetailText" parent="Widget.AppCompat.TextView">
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">@color/black_100</item>
    </style>
</resources>
